// import { selectFavouriteBeersDomain } from '../selectors';

describe('selectFavouriteBeersDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
